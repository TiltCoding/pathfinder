# Исследование кодовой базы: <название задачи>

> Заполняется агентами `wf-explorer`. Каждый исследователь добавляет свою секцию, не затирая чужие.
> Сначала читаем `docs/knowledge/INDEX.md` и переиспользуем известное.

## Область: <фокус исследователя>

### Ключевые файлы
- `path/to/file.ext:42` — <роль в одну строку>

### Точки входа и поток
<Как управление/данные попадают в эту область.>

### Что переиспользовать
- `path/to/util.ext` — <существующая функция/паттерн, на которую опираемся>

### Дизайн-система / UI
> Только для UI-задач — чтобы макеты демо были достоверны. Иначе пропустить.
- <дизайн-токены: цвета, шрифты, отступы — где заданы>
- <библиотека компонентов / UI-kit и ключевые компоненты>
- <точки входа UI: страницы/экраны/роуты — `path:line`>

### Ограничения и риски
- <инварианты, связность, что может сломаться>

### Команды
- Тесты: `<команда>`
- Сборка/линт: `<команда>`

### Открытые вопросы
- <конкретный вопрос для фазы проработки>
